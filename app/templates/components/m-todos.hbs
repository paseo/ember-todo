<ul class="todo-list list-unstyled">
	<li>
		{{input class="form-control im" placeholder="输入代办内容,按Enter添加" enter="addTodo" escape-press="cancelAdd"}}
	</li>

	{{#each items as |item| }}
		{{m-todo record=item id=item.id title=item.title isCompleted=item.isCompleted}}
	{{/each}}
</ul>
